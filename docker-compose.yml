services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: farmguard-backend
    env_file:
      - ./backend/.env
    environment:
      SQLITE_PATH: /data/db.sqlite3
    ports:
      - "8000:8000"
    volumes:
      - backend_sqlite:/data
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_BACKEND_BASE_URL: ${VITE_BACKEND_BASE_URL:-https://farmguard-01jl.onrender.com}
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
    container_name: farmguard-frontend
    depends_on:
      - backend
    ports:
      - "5173:80"
    restart: unless-stopped

volumes:
  backend_sqlite:
